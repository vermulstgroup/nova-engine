---
summary: "CLI reference for `nova-engine uninstall` (remove gateway service + local data)"
read_when:
  - You want to remove the gateway service and/or local state
  - You want a dry-run first
title: "uninstall"
---

# `nova-engine uninstall`

Uninstall the gateway service + local data (CLI remains).

```bash
nova-engine uninstall
nova-engine uninstall --all --yes
nova-engine uninstall --dry-run
```
